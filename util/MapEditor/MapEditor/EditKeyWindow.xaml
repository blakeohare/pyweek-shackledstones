<Window x:Class="MapEditor.EditKeyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EditKeyWindow" Height="300" Width="300">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="30" />
			<RowDefinition Height="50" />
		</Grid.RowDefinitions>

		<TextBox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" x:Name="key_value" AcceptsReturn="True"  />
		
		<Button Grid.Row="1" x:Name="done">Done</Button>
		<TextBlock Grid.Row="2" Text="Note: editing anything that appears or influences the map surface will not automatically refresh. Reload the map to see changes." TextWrapping="Wrap" />
	</Grid>
</Window>
